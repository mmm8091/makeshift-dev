# 首页内容与素材需求清单

> 这是一份**给内容作者（你）的需求规格**。开发侧已搭好首页骨架与暖纸墨绿视觉，
> 现在缺的是真实文案与首页主图。请按下面的槽位填空、出图；填完交回，我直接接进代码。
> 字数上限是版式约束（超了会破排版），不是必须写满。

---

## A. 首页主图（1 张，你出图）

- **用途**：首页 Hero 右侧主视觉（也会用于 OG 分享图来源）
- **母题（已定）**：野草长成电路 —— logo 放大成纪念碑式木刻
- **画面建议**：一株巨大的野草从黑土里长出，主干粗壮向上；枝桠在顶端逐渐"木刻化"为电路 / 管线 / 结构，金色节点泛微光；草根处可放一张小木桌、一盏暖灯、一个坐着的小人背影（呼应"小人 + 巨物"）。
- **风格**：严格沿用 `docs/草台编子识字班插画美术规范.md` 的重黑红木刻，**不要**为迁就页面调亮。
- **输出规格**：

  | 项 | 要求 |
  |---|---|
  | 比例 | 横向 16:9（首页按 16:9 裁切显示） |
  | 分辨率 | ≥ 2400×1350（首页大图，要清晰） |
  | 格式 | PNG |
  | 文件名 | `首页主视觉-野草电路.png` |
  | 存放 | `设计资产/首页/`（新建目录）；定稿后我拷进 `public/illustrations/hero-home.png` |

- **可直接用的生成 prompt（已按美术规范模板填好）**：

  ```text
  Use case: stylized-concept
  Asset type: homepage hero illustration, motif "wild grass growing into a circuit"
  Primary request: a single monumental wild grass plant rising from black earth; its thick stalk grows upward and its top branches gradually turn into carved circuit traces, pipes and structure with faint glowing golden nodes; at the roots, a tiny wooden desk, a warm lamp and the back of a small seated person.
  Style/medium: digital-era worker woodcut, industrial allegory, Chinese new woodcut influence, rough black carved lines, screenprint grain, high contrast, monumental and solemn, not commercial, not explanatory.
  Composition/framing: landscape 16:9, monumental central plant silhouette, low-angle view, strong silhouette, clear focal point, readable from a distance.
  Lighting/mood: deep red dawn or dusk light behind the plant, oppressive dark structure, sparse cold green and gold node glow, warm highlight on the small desk and lamp.
  Color palette: black ink, charcoal gray, old wood brown, muted paper cream, deep red, sparse terminal green, gold nodes.
  Constraints: no readable text, no logo, no watermark, no robots, no glossy 3D, no infographic labels, no floating UI panels, no cute style, no commercial marketing look, avoid clutter unless it forms a clear rhythmic structure.
  ```

- **第一版只需这 1 张**。下方"宣言带"如保留，可继续复用已有的 `矿脉` 图做压暗底，不必新出。

---

## B. 首页文案（你填空）

每个槽位标了 **[必填]/[可选]** 与字数上限。括号里是现在的占位文字，供参考，可改可推翻。

### 1. 站点定位（用于浏览器标签/分享卡片）
- [必填] 一句话定位 ≤ 60 字
  （现：面向普通人的数字时代识字班：平民编程、AI 编程、互助学习。学会用 AI 和代码完成真实创造。）
  → **你的版本：**

### 2. Hero 主视觉区
- [必填] 眉标 kicker ≤ 16 字（现："平民编程 · AI 编程 · 互助学习"）
  → 
- [必填] 主标题 H1 ≤ 14 字，可两行（现："数字时代的识字班"）
  → 
- [必填] 副文案 ≤ 80 字（现："这不是一个把课程包装得很贵的培训站。它是一所夜校……"）
  → 
- [必填] 主按钮文案 ≤ 8 字（现："先读前言 →"）
  → 
- [必填] 次按钮文案 ≤ 8 字（现："用卡密解锁课程"）
  → 
- [可选] 按钮下小注 ≤ 36 字（现："从第一课起，你就会提交一次真实的 GitHub PR……"）
  → 

### 3. 宣言带（深色木刻区，"为什么是现在"）
> **结构待你定**：这一带现在是 3 根偏泛的支柱（技术祛魅 / 数字主权 / 互助），我觉得偏占位。
> 选项：① 重写成你真正想说的 3 点；② 砍掉整带；③ 换成别的内容。先在这告诉我留不留。
- [可选] 眉标 ≤ 12 字（现："为什么是现在"）→ 
- [可选] 大标题 ≤ 40 字，可两行（现："认知劳动正在被重新定价。这一次，普通人该重新拿起工具。"）→ 
- [可选] 三柱（各：小标题 ≤ 6 字 + 一句 ≤ 44 字）
  - 柱1 标题 → ___ ｜ 一句 → 
  - 柱2 标题 → ___ ｜ 一句 → 
  - 柱3 标题 → ___ ｜ 一句 → 

### 4. 课程目录区
- [必填] 区标题 ≤ 16 字（现："一条从念头到作品的路"）→ 
- [必填] 一句引导 ≤ 50 字（现："目录、标题与摘要公开；正文受卡密保护……前言永远免费。"）→ 
- [必填] **真实课程清单** → 见下方 C 节模板

### 5. 学员墙区
- [必填] 区标题 ≤ 16 字（现："草台是这些人一起搭的"）→ 
- [必填] 一句说明 ≤ 50 字（现："第一课的作业，就是提交一次真实的 PR，把自己的头像放上这面墙。"）→ 

### 6. 报名 CTA 区
- [必填] 标题 ≤ 30 字，可两行（现："从消费者，到创造者。这一步不难，难的是开始。"）→ 
- [必填] 副文案 ≤ 80 字（现："注册时填写昵称、GitHub 用户名和 QQ 号……"）→ 
- [必填] 主按钮 ≤ 8 字（现："注册账号"）→ 
- [必填] 次按钮 ≤ 8 字（现："我已有卡密"）→ 

### 7. 页脚
- [必填] 一句标语 ≤ 50 字（现："一个面向普通人的数字时代识字班。平民编程、AI 编程、互助学习 —— 从消费者到创造者。"）→ 

---

## C. 真实课程清单（你填）

每门一行，按这个格式给我（标题 ≤ 20 字，摘要 ≤ 44 字）：

```
顺序 | 标题 | 一句摘要 | 公开免费 还是 需卡密
0 | 前言：平民编程与创造的时代 | <摘要> | 公开
1 | <标题> | <摘要> | 需卡密
2 | <标题> | <摘要> | 需卡密
...
```

- 第一版打算上几门？（哪怕只敲定 3–5 门暂定也行，先把结构跑起来）
- 哪些公开试看、哪些需卡密？（前言默认公开免费）

---

## D. 几个结构决策（你顺手定，影响要不要加/删板块）

1. **宣言带**留不留？（见 B-3）
2. 要不要加一个 **"招生说明 / 价格 / 卡密怎么买"** 板块？还是这些只放在单独的 `/redeem` 页？
3. 要不要加一个 **"这门课适合谁 / 不适合谁"** 的诚实板块？（很贴草台气质，但非必须）
4. 首页板块顺序现在是：Hero → 宣言带 → 课程目录 → 学员墙 → 报名。要调顺序就在这写。

---

填好交回（直接在本文件改，或贴给我都行）。图出好了放 `设计资产/首页/`。
我拿到后：替换主图、把文案接进各组件、按你的结构决策增删板块。
